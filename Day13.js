function test(){
    const studentName = {
        name: "SHAWN MICHAEL SUDARIA",
        age: 18,
        degree_program: "BSIT",
        school: "EVSU"
    }

    console.log(`${studentName}\n`);
    console.log(`Hello I am ${studentName.name} and I am now ${studentName.age} taking ${studentName.degree_program} at ${studentName.school}`)
}

function concatStrings(a, b){
    return a + b;
}

function displayerPatters(){
    const promp = requre("prompt-sync")({sigint: true});
    
}

console.log(concatStrings("Cheese", "cake"));